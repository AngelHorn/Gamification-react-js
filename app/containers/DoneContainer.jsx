import React, { PropTypes } from 'react'

const DoneContainer = React.createClass({
    render () {
        return (
            <div>DoneContainer</div>

        )
    }
})

export default DoneContainer
